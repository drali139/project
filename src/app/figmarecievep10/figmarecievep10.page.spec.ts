import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Figmarecievep10Page } from './figmarecievep10.page';

describe('Figmarecievep10Page', () => {
  let component: Figmarecievep10Page;
  let fixture: ComponentFixture<Figmarecievep10Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Figmarecievep10Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
