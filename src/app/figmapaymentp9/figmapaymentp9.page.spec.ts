import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Figmapaymentp9Page } from './figmapaymentp9.page';

describe('Figmapaymentp9Page', () => {
  let component: Figmapaymentp9Page;
  let fixture: ComponentFixture<Figmapaymentp9Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Figmapaymentp9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
