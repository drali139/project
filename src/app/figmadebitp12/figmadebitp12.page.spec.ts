import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Figmadebitp12Page } from './figmadebitp12.page';

describe('Figmadebitp12Page', () => {
  let component: Figmadebitp12Page;
  let fixture: ComponentFixture<Figmadebitp12Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Figmadebitp12Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
